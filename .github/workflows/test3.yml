name: test g
on:
  workflow_dispatch:
    inputs:
      mon_input:
        description: 'mon input'
        required: true
        default: 'valeur par defaut'
        type: string



jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
        - name: date
          run: date
        - name: date 1
          run: docker -v
        - name: date 1
          run: docker -v
        - name: Checkout
          uses: actions/checkout@v4
#        - name: build
#          run: docker build -t test1 .
#        - name: lance docker
#          run: docker run -it test1 


          


#        - name: cowsay t
#          run: |
#            sudo apt update
#            sudo apt install -y cowsay 

#        - name: cowsay2 
#          run: sudo mv /usr/share/cowsay /apps

#        - name: cowsay
#          run: co
