name: test g
on:
  workflow_dispatch:

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
        - name: date
          run: date
        - name: date 1
          run: docker -v
        - name: date 1
          run: docker -v
        - name: Checkout
          uses: actions/checkout@v4

#        - name: install kubectl
#          run: apt update && apt install kubectl -y

        - name: config kubeconfig
          run: echo "${{ secrets.KUBECONFIG }}" > /home/.kube/config/k3s.yaml

        - name: kubectl get node
          run: export KUBECONFIG=/home/.kube/config/k3s.yaml && kubectl get node  

#        - name: build
#          run: docker build -t test1 .
#        - name: lance docker
#          run: docker run -it test1 


          


#        - name: cowsay t
#          run: |
#            sudo apt update
#            sudo apt install -y cowsay 

#        - name: cowsay2 
#          run: sudo mv /usr/share/cowsay /apps

#        - name: cowsay
#          run: co
