name: test g
on:
  workflow_dispatch:
  push:

jobs:
    wireguard:
        runs-on: ubuntu-latest
        steps:
        - name: date
          run: date
        - name: date 1
          run: docker -v
        - name: date 1
          run: docker -v
#        - name: Checkout
#          uses: actions/checkout@v4

        - name: test curl
          run: curl https://8.8.8.8

#        - name: test curl2
#          run: curl -Ik ${{ secrets.IP2 }}

#        - name: install wireguard
#          run: apt update && apt install wireguard -y

        - name: install cli argocd
          run: |
            curl -sSL -o argocd-linux-amd64 https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
            sudo install -m 555 argocd-linux-amd64 /usr/local/bin/argocd
            rm argocd-linux-amd64

        - name: install helm
          run: |
            curl -sSL -o get_helm.sh https://raw.githubusercontent.com/helm/helm/master/scripts/get-helm-3
            chmod 700 get_helm.sh
            ./get_helm.sh

          

#        - name: test wget
#          run: wget ${{ secrets.IP2 }} --no-check-certificate

#        - name: install kubectl
#          run: apt update && apt install kubectl -y

#        - name: config kubeconfig
#          run: sudo echo "${{ secrets.KUBECONFIG }}" >> sudo /home/k3s.yaml

#        - name: kubectl get node
#          run: sudo export KUBECONFIG=/home/k3s.yaml && kubectl get node  

#        - name: build
#          run: docker build -t test1 .
#        - name: lance docker
#          run: docker run -it test1 


          


#        - name: cowsay t
#          run: |
#            sudo apt update
#            sudo apt install -y cowsay 

#        - name: cowsay2 
#          run: sudo mv /usr/share/cowsay /apps

#        - name: cowsay
#          run: co
